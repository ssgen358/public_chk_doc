# このリポジトリについて

設計書・ドキュメントの点検ツール群を整備するプロジェクト。
各チェック観点ごとにフォルダを分け、汎用Pythonスクリプトを配置していく。

## フォルダ構成

### チェックツール

観点ごとにフォルダを分割する。フォルダ名は `NN_check_` プレフィックスで統一する（NNは2桁の番号）。
番号はチェックを実施する推奨順序を示す。

```
01_check_format/        フォーマット準拠（テンプレート構造チェック）
02_check_required/      必須項目の未入力・空欄検出
03_check_charset/       文字種チェック（全角/半角混在等）
04_check_notation/      表記揺れ・用語統一
05_check_naming/        命名規約違反の検出
06_check_duplicate/     重複定義・重複記述の検出
07_check_consistency/   設計書間の整合性チェック
08_check_existence/     実態との突合チェック（設計書記載 vs 実ファイル等）
09_check_ai_review/     AIによる品質レビュー
```

### ユーティリティ

チェックの前処理・補助ツールは `00_utils/` 配下に配置する。
チェックツールとは明確に分離する。フォルダ番号は `00` で統一する。

```
00_utils/
├── file_list/          フォルダ配下のファイルを一覧化（CSV出力）
└── excel_extract/      Excelの指定シートからデータ行を抽出・マージ（CSV出力）
```

## コーディング方針

- **言語**: Python
- **対象ファイル形式**: Excel（.xlsx）、Word（.docx）、CSV等を想定
- **1ツール1観点**: 複数の観点を1スクリプトに詰め込まない
- **汎用性優先**: 特定の設計書フォーマットに依存しすぎない作りにする
- **出力**: チェック結果はコンソール出力またはCSV出力とする
- **引数**: フォルダパス・ファイルパス等はコマンドライン引数で受け取る

## PythonとAIの役割分担

- **Python**: 機械的に判定できるチェック（必須項目、重複、命名規約、表記揺れ、突合）
- **AI（Claude API等）**: 意味理解が必要なチェック（説明不足、曖昧表現、観点漏れ）

## 作業スタイル

- フォルダ構成は段階的に育てていく（最初から完璧にしない）
- ツールを追加するたびに `README.md` のツール一覧を更新する
- 各スクリプト先頭に概要・使い方のコメントを記載する
